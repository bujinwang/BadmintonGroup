import 'react-native-url-polyfill/auto';
/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';

// Register the main component with the name 'main' to match the error
AppRegistry.registerComponent('main', () => App);
